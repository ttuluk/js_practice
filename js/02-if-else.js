/* let day = mon;

if (day = mon) {
    alert('workday');
} else if (day = mon) {
    alert('workday');
} else if (day = mon) {
    alert('workday');
} else if (day = mon) {
    alert('workday');
} else if (day = mon) {
    alert('workday');
}

 */
let message = 'Curabitur ligula sapien';
console.log(message.length);
const maxLength = 23;
console.log(message.slice(0, 15));
let result;

if (message.lenght < maxLength message.length<=maxLength) {
    result = string(message.slice(0, maxLength)) + '...';
    console.log(result);
}
else {
    result = message
console.log(result);}

function formatMessage(message, maxLength) {
  let result;
  // Change code below this line
if (message.lenght > maxLength) {
    result = message
    console.log(result);
}
  
 else {
    result = message.slice(0, maxLength) + '...';
console.log(result);}

  /// Change code above this line
  return result;
}